version: '3.8'

networks:
  net:
    
volumes:
  nginx_log_volume:
    
services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./unicode.mapping:/etc/modsecurity/unicode.mapping:ro
      - /etc/letsencrypt/live:/etc/letsencrypt/live:ro
      - /etc/letsencrypt/archive:/etc/letsencrypt/archive:ro
      - /etc/letsencrypt/options-ssl-nginx.conf:/etc/letsencrypt/options-ssl-nginx.conf:ro
      - /etc/letsencrypt/ssl-dhparams.pem:/etc/letsencrypt/ssl-dhparams.pem:ro
      - nginx_log_volume:/var/log/nginx/
    depends_on:
      - chat-flow
    networks:
      - net
        
  nginx-exporter:
    build:
      context: .
      dockerfile: Dockerfile.nginx-exporter
    ports:
      - "4040:4040"
    volumes:
      - nginx_log_volume:/var/log/nginx/
    networks:
      - net
    
  chat-flow:
    image: chatflow-service:latest
    restart: always
    networks:
      - net