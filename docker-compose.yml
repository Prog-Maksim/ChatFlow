version: '3.8'

services:
  debug:
    image: alpine
    command: ["sh", "-c", "env | grep VAULT || echo 'No VAULT vars'"]
    environment:
      - Vault__Address=${VAULT_ADDR}
      - Vault__RoleId=${VAULT_ROLE_ID}
      - Vault__SecretId=${VAULT_SECRET_ID}
